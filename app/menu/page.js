import { MenuExperience } from "@/components/menu-experience";

export const metadata = {
  title: "Menu | Amazing Chinese Restaurant"
};

export default function MenuPage() {
  return <MenuExperience />;
}
