import { AboutSection } from "@/components/about-section";

export const metadata = {
  title: "About Us | Amazing Chinese Restaurant"
};

export default function AboutPage() {
  return <AboutSection standalone />;
}
